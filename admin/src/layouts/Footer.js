import React from 'react';
import { Layout, Button } from 'antd';

const { Footer } = Layout;
const FooterView = () => {
    return <Footer>
        <Button>Footer</Button>
    </Footer>
}


export default FooterView;